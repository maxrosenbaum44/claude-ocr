# Deliberately small: no PyTorch, no model downloads, nothing hosted by a third
# party that can move. onnxruntime wheels are Microsoft-signed, so they pass
# Windows Smart App Control; RapidOCR bundles its models inside the wheel.
pymupdf>=1.24
rapidocr-onnxruntime>=1.4
